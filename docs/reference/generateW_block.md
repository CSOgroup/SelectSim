# Generating the weight matrix taking sample covariate

Generating the weight matrix taking sample covariate

## Usage

``` r
generateW_block(al, lambda, tau)
```

## Arguments

- al:

  altearting landscape onject

- lambda:

  penalty factor parameter

- tau:

  fold change parameter

## Value

Weight matrix
